
#include <bits/stdc++.h>

#define FASTIO                             \
    std::ios_base::sync_with_stdio(false); \
    std::cin.tie(NULL)

int tc;

int main()
{
    // FASTIO;
    std::cin >> tc;
    while (tc--)
    {
        int n, m;
        std::cin >> n >> m;
        for (int i = 0; i < m; ++i)
        {
            int st, en;
            std::cin >> st >> en;
        }

        std::cout << n - 1 << "\n";
    }

    int nn;
    std::cin >> nn;
    return 0;
}